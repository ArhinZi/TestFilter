Test Filter Driver Project
==========================

This project contains the code for the filter driver.

The various folder ending with "build" contain the SOURCE and MAKEFILE
for the build for a specific platform.

To build the driver, run build -gcwZ in the DDK build command prompt for 
the platform the subdirectory is for.